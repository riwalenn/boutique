{% extends 'base.html.twig' %}

{% block title %}Mes adresses - La Boutique{% endblock %}

{% block content %}
    <a href="{{ path('account_address') }}">Retourner à mes adresses</a>
    <h1>Mes adresses - ajouter une adresse</h1>
    Bienvenue dans votre compte ! C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.

    <hr>
    {{ form_start(form) }}
    <div class="row">
        <div class="form-group col-md-6">{{ form_row(form.name) }}</div>
        <div class="form-group col-md-6">{{ form_row(form.phone) }}</div>
    </div>
    <div class="row">
        <div class="form-group col-md-6">{{ form_row(form.lastname) }}</div>
        <div class="form-group col-md-6">{{ form_row(form.firstname) }}</div>
    </div>
    <div class="row">
        <div class="form-group col-md-12">{{ form_row(form.address) }}</div>
    </div>
    <div class="row">
        <div class="form-group col-md-6">{{ form_row(form.zipcode) }}</div>
        <div class="form-group col-md-6">{{ form_row(form.city) }}</div>
    </div>
    <div class="row">
        <div class="form-group col-md-6">{{ form_row(form.company) }}</div>
        <div class="form-group col-md-6">{{ form_row(form.country) }}</div>
    </div>
    <div class="row">
        <div class="form-group col-md-3 my-auto">{{ form_row(form.submit) }}</div>
        <div class="form-group col-md-9 my-auto">{{ form_row(form.byDefault) }}</div>
    </div>

    {{ form_end(form) }}
{% endblock %}
